
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#include "MBAR.h"
#include "EF.h"

int main(int argc, char *argv[]) {
  int i,j;
  int flag=0;
  
  char *inputfilename,*inputfilename2,*inputfilename3,*outputfilename;
  FILE *inputfile,*inputfile2, *outputfile;
  
  if (argc < ) {
    printf("USAGE: ./.exe flag() inputfilename() inputfilename2() outputfilename\n");
    exit(1);
  }
  flag=(*++argv)[0];
  if (flag != ' ' && flag != ' ') {
    printf("flag error: must be   or  ");
    exit(1);
  }
  inputfilename =  *++argv;
  inputfilename2 = *++argv;
  inputfilename3 = *++argv;
  outputfilename = *++argv;
  
  inputfile2=efopen(inputfilename2,"r");
  
  fclose(inputfile2);
  
  inputfile1=efopen(inputfilename1,"r");
  
  fclose(inputfile1);
  
  outputfile=efopen(outputfilename,"w");
  
  fclose(outputfile);
  
  return 0;
}

