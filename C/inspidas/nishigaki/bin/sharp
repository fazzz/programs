#
# shar like file transfer from UNIX to PC
#
if($#argv == 0) then
    echo "Usage : $0 file1 file2 ... > transfile"
    exit
endif
while($#argv)
    echo "*O*R*G* $argv[1]"
    cat $argv[1]
    echo "*E*O*F*"
    shift
end
